<Window x:Class="ImageCompressor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="ImageCompressor 1.0"
        Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#f8fafc"
        Foreground="#1e293b"
        FontFamily="Segoe UI, Microsoft YaHei, sans-serif">

    <Window.Resources>
        <!-- Color Palette - Light Theme -->
        <SolidColorBrush x:Key="BackgroundBase" Color="#f8fafc"/>
        <SolidColorBrush x:Key="CardBackground" Color="#ffffff"/>
        <SolidColorBrush x:Key="BorderColor" Color="#e2e8f0"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#3b82f6"/>
        <SolidColorBrush x:Key="PrimaryHover" Color="#2563eb"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#10b981"/>
        <SolidColorBrush x:Key="WarningColor" Color="#f59e0b"/>
        <SolidColorBrush x:Key="ErrorColor" Color="#ef4444"/>
        <SolidColorBrush x:Key="TextMain" Color="#1e293b"/>
        <SolidColorBrush x:Key="TextMuted" Color="#64748b"/>
        <SolidColorBrush x:Key="TextLight" Color="#94a3b8"/>
        <SolidColorBrush x:Key="HoverBackground" Color="#f1f5f9"/>

        <!-- Shadows -->
        <DropShadowEffect x:Key="CardShadow" BlurRadius="12" ShadowDepth="0" Color="#000000" Opacity="0.08"/>
        <DropShadowEffect x:Key="ButtonShadow" BlurRadius="8" ShadowDepth="2" Color="#3b82f6" Opacity="0.2"/>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="600"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="32,0,32,0"/>
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHover}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1d4ed8"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Width" Value="60"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="500"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,0,16,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBackground}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="500"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,0,16,0"/>
            <Setter Property="Background" Value="#ef4444"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#dc2626"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#b91c1c"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Text Styles -->
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="600"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="SubtitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="500"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="BodyStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
        </Style>

        <Style x:Key="SmallTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
        </Style>

        <!-- Input Style -->
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="500"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="12,0,12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0,0,0,0" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                    <Setter Property="Background" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Radio Button Style -->
        <Style x:Key="RadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="500"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- ComboBox Item Style -->
        <Style x:Key="ComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <ContentPresenter VerticalAlignment="Center"
                                                HorizontalAlignment="Left"
                                                Margin="12,0,12,0"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBackground}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBackground}"/>
                    <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Drop Zone Style -->
        <Style x:Key="DropZoneStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="24"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="图片压缩工具" Style="{StaticResource TitleStyle}"/>
            <TextBlock Text="专业级图片压缩解决方案" Style="{StaticResource SmallTextStyle}"/>
        </StackPanel>

        <!-- Upload Zone -->
        <Border Grid.Row="1" x:Name="DropZone"
                Style="{StaticResource DropZoneStyle}"
                Height="180"
                DragOver="DropZone_DragOver" Drop="DropZone_Drop"
                MouseEnter="DropZone_MouseEnter" MouseLeave="DropZone_MouseLeave">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Center">
                    <TextBlock x:Name="DragIcon" Text="📁"
                               FontSize="40" Margin="0,0,0,8"/>
                    <TextBlock x:Name="DragText" Text="拖放图片到这里"
                               FontSize="13" FontWeight="600"
                               Foreground="{StaticResource TextMain}"/>
                    <TextBlock x:Name="StatusText" Text="支持 JPG, PNG, GIF, BMP 格式"
                               FontSize="11" Foreground="{StaticResource TextLight}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center"
                            VerticalAlignment="Center" Margin="0,0,12,0">
                    <TextBlock x:Name="AddedCountText" FontSize="12"
                               FontWeight="500" Foreground="{StaticResource TextMuted}"
                               Margin="0,0,12,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Vertical" HorizontalAlignment="Right">
                    <Button x:Name="BrowseFilesButton" Content="添加文件"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Height="36" Width="120" Click="BrowseFilesButton_Click"/>
                    <Button x:Name="OpenDirectoryButton" Content="添加文件夹"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Height="36" Width="120" Margin="0,8,0,0" Click="OpenDirectoryButton_Click"/>
                    <Button x:Name="ClearButtonLink" Content="清空"
                            Style="{StaticResource DangerButtonStyle}"
                            Height="36" Width="120" Margin="0,8,0,0" Click="ClearButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Settings Card -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,16,0,16">
            <StackPanel>
                <TextBlock Text="压缩设置" Style="{StaticResource SubtitleStyle}"/>

                <!-- Target Size -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="目标大小"
                               Margin="0,0,5,0"
                               VerticalAlignment="Center"
                               Style="{StaticResource SmallTextStyle}"/>

                    <ComboBox x:Name="TargetSizeComboBox" Grid.Column="1" Height="36"
                              FontSize="13" FontWeight="500" Foreground="{StaticResource TextMain}"
                              BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                              Background="Transparent" Padding="12,0,12,0"
                              VerticalContentAlignment="Center"
                              ItemContainerStyle="{StaticResource ComboBoxItemStyle}"
                              SelectionChanged="TargetSizeComboBox_SelectionChanged">
                        <ComboBoxItem Content="100 KB" Tag="100"/>
                        <ComboBoxItem Content="200 KB" Tag="200"/>
                        <ComboBoxItem Content="500 KB" Tag="500"/>
                        <ComboBoxItem Content="1 MB" Tag="1024"/>
                        <ComboBoxItem Content="2 MB" Tag="2048"/>
                        <ComboBoxItem Content="5 MB" Tag="5120"/>
                        <ComboBoxItem Content="自定义..." Tag="custom"/>
                    </ComboBox>

                    <TextBox x:Name="CustomSizeTextBox" Grid.Column="2" Width="130" Height="40"
                             Style="{StaticResource InputStyle}"
                             Visibility="Collapsed" TextChanged="CustomSizeTextBox_TextChanged"/>
                </Grid>

                <!-- Compression Mode -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <RadioButton x:Name="CompressionModeBest" Content="极致压缩"
                                 Style="{StaticResource RadioButtonStyle}"
                                 GroupName="Mode" IsChecked="True" Click="CompressionModeBest_Click"/>
                    <RadioButton x:Name="CompressionModeGood" Content="高质量压缩"
                                 Style="{StaticResource RadioButtonStyle}"
                                 GroupName="Mode" Click="CompressionModeGood_Click"/>
                </StackPanel>

                <!-- Output Path -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- <TextBlock Grid.Column="0" Text="输出路径"
                                Height="36"
                               VerticalAlignment="Center"
                               Style="{StaticResource SmallTextStyle}"/> -->

                    <TextBlock Grid.Column="0" Text="输出路径"
                               Margin="0,0,5,0"
                               VerticalAlignment="Center"
                               Style="{StaticResource SmallTextStyle}"/>

                    <TextBox x:Name="OutputPathTextBox" Grid.Column="1" Height="36"
                             FontSize="12" FontWeight="500"
                             Foreground="{StaticResource TextMain}"
                             BorderBrush="{StaticResource BorderColor}"
                             VerticalContentAlignment="Center"
                             BorderThickness="1" Background="Transparent" Padding="10,0,10,0"
                             TextChanged="OutputPathTextBox_TextChanged"/>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button x:Name="SelectOutputPathButton" Content="浏览"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="2,0,2,0" Height="36" Click="SelectOutputPath_Click"/>
                        <Button x:Name="OpenOutputPathButton" Content="打开"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Height="36" Click="OpenOutputPath_Click"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Compress Button -->
        <Grid Grid.Row="3">
            <Button x:Name="CompressButton" Style="{StaticResource PrimaryButtonStyle}"
                    Content="开始压缩" Click="CompressButton_Click"/>
        </Grid>

        <!-- Results Card -->
        <Border Grid.Row="4" x:Name="ResultBorder"
                Background="{StaticResource CardBackground}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                Padding="24" Visibility="Collapsed"
                CornerRadius="8" Margin="0,16,0,0">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock x:Name="ResultTitle" Text="✓ 压缩完成"
                               FontSize="14" FontWeight="600"
                               Foreground="{StaticResource SuccessColor}"
                               Margin="0,0,10,0"/>
                    <TextBlock x:Name="ImageCountText" FontSize="12" FontWeight="500"
                               Foreground="{StaticResource TextMuted}"/>
                </StackPanel>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Column="0" Grid.Row="0" Text="原始大小"
                               Style="{StaticResource SmallTextStyle}"/>
                    <TextBlock x:Name="AvgOriginalSizeText" Grid.Column="0" Grid.Row="1"
                               FontSize="13" FontWeight="600"
                               Foreground="{StaticResource TextMain}"/>

                    <TextBlock Grid.Column="1" Grid.Row="0" Text="压缩后"
                               Style="{StaticResource SmallTextStyle}"/>
                    <TextBlock x:Name="AvgCompressedSizeText" Grid.Column="1" Grid.Row="1"
                               FontSize="13" FontWeight="600"
                               Foreground="{StaticResource SuccessColor}"/>

                    <TextBlock Grid.Column="2" Grid.Row="0" Text="节省"
                               Style="{StaticResource SmallTextStyle}"/>
                    <TextBlock x:Name="AvgCompressionRateText" Grid.Column="2" Grid.Row="1"
                               FontSize="13" FontWeight="600"
                               Foreground="{StaticResource WarningColor}"/>
                </Grid>

                <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                        CornerRadius="6" Padding="12" Background="#f8fafc">
                    <TextBlock x:Name="DetailsText" FontSize="11"
                               Foreground="{StaticResource TextMuted}"
                               TextWrapping="Wrap"/>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</Window>